var students = [true, "Harshit", "Kumar", "Raghu", "Salma", "Uttam"];

console.log(students)
console.log(students.length)

students[5] = "uma"

students.push("jagrav")

students.push(35) // will add the new element at the end
students.pop(); // removes element at the end
students.shift() // removes elelemnt in the begining
students.unshift('Manasvi') // will add the new element at the begining

console.log(students)
